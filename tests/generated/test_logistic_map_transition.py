"""Generated by PhaseProbe from a validated replay fixture."""

from pathlib import Path

from phaseprobe.replay import verify_replay

FIXTURE = Path(__file__).parent / "fixtures" / "logistic_map-replay.json"


def test_logistic_map_transition_replays() -> None:
    """Re-execute model/config/seed and verify class and exact retained trace hashes."""
    result = verify_replay(FIXTURE)
    assert result.ok, result.as_dict()
